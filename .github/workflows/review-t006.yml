name: T006 Independent Review

on:
  push:
    branches:
      - review/p0-T006

permissions:
  contents: read

jobs:
  acceptance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0

      - name: Clean install
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Existing tests
        run: npm run test

      - name: Independent movement tests
        run: npx vitest run tests/review-t006.test.ts

      - name: Simulation boundaries
        shell: bash
        run: |
          ! grep -R -nE "Date\.now|Math\.random|setTimeout|setInterval|from ['\"]phaser['\"]|from ['\"]@junqverse/protocol['\"]|window\.|document\." packages/sim/src

      - name: Planning integrity
        run: npm run test:plan

      - name: Build
        run: npm run build
